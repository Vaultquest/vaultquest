# Soroban DripPool operation cost budget thresholds.
# Measured in native test environment. Format: key_cpu or key_mem.
# Baseline values re-measured against the repaired, compiling contract (the
# earlier baseline predated the participant-registry and signer-set-hash work
# and could never be validated while the crate did not build). Each limit is the
# measured cost plus modest headroom; tighten if a change lowers real cost.

create_cpu=110000
create_mem=13000

join_cpu=125000
join_mem=20000

deposit_cpu=160000
deposit_mem=24000

drip_cpu=160000
drip_mem=24000

draw_winner_cpu=155000
draw_winner_mem=21000

claim_cpu=110000
claim_mem=17000

withdraw_cpu=180000
withdraw_mem=24000

propose_cpu=145000
propose_mem=24000

approve_cpu=245000
approve_mem=42000
